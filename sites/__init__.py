"""
Site packages live under this folder (views, templates, static, optional urls).

``verso``, ``chronicle``, and ``khula`` are Django apps under ``sites`` with their
own models. Folders like ``driftglass`` hold templates/static only; routing and
models live in ``core``.
"""
